---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Myan Patel — SRE / Cloud / Automation"
  description="Site Reliability Engineer focused on cloud automation, reliability, and network/security platforms."
>
  <!-- HERO -->
  <section class="grid gap-10 lg:grid-cols-12 lg:items-start">
    <div class="lg:col-span-7">
      <div class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1 text-xs text-slate-300">
        <span class="h-2 w-2 rounded-full bg-emerald-400"></span>
        Available for new opportunities in 2026
      </div>

      <h1 class="mt-5 text-4xl font-semibold tracking-tight sm:text-5xl">
        I build and automate cloud infrastructure.
      </h1>

      <p class="mt-4 max-w-2xl text-base leading-relaxed text-slate-300 sm:text-lg">
        I’m an SRE at BT working across cloud, automation, and network/security tooling.
        I focus on reliability, repeatable operations, and practical engineering that scales.
      </p>

      <!-- compact “chips” instead of 3 cards -->
      <div class="mt-6 flex flex-wrap gap-2">
        {["Automation + Reliability", "AWS • Terraform • Python", "Network/Security Platforms"].map((t) => (
          <span class="rounded-full bg-white/5 px-3 py-1 text-xs text-slate-200 ring-1 ring-white/10">
            {t}
          </span>
        ))}
      </div>

      <div class="mt-7 flex flex-col gap-3 sm:flex-row">
        <a
          href="/projects/"
          class="inline-flex items-center justify-center rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-slate-950 hover:bg-slate-200"
        >
          View projects
        </a>
        <a
          href="/contact/"
          class="inline-flex items-center justify-center rounded-xl border border-white/15 bg-white/5 px-4 py-2.5 text-sm font-semibold text-white hover:bg-white/10"
        >
          Contact
        </a>
      </div>
    </div>

    <!-- ONE supporting panel -->
    <aside class="lg:col-span-5">
      <div class="rounded-3xl border border-white/10 bg-white/5 p-6">
        <h2 class="text-lg font-semibold tracking-tight">Focus</h2>

        <div class="mt-4 space-y-3">
          <div class="card bg-slate-950/30 p-4">
            <p class="text-sm font-semibold">Automation & scripting</p>
            <p class="mt-1 text-sm text-slate-300">Python tooling to automate operational workflows and integrate with platform APIs.</p>
          </div>

          <div class="card bg-slate-950/30 p-4">
            <p class="text-sm font-semibold">Cloud + IaC</p>
            <p class="mt-1 text-sm text-slate-300">Terraform modules, environments, repeatable builds.</p>
          </div>

          <div class="card bg-slate-950/30 p-4">
            <p class="text-sm font-semibold">Platform integration</p>
            <p class="mt-1 text-sm text-slate-300">Automating and integrating with network and security platforms as part of broader cloud workflows.</p>
          </div>
        </div>
      </div>
    </aside>
  </section>
</BaseLayout>